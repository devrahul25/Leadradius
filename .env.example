# Google Places API key — required for live lead search.
# Frontend prototype only. In production, proxy this through your backend
# so the key is never exposed to the client.
NEXT_PUBLIC_GOOGLE_API_KEY=

# Toggle: set to "true" to call the real Google Places API.
# Otherwise the app uses lib/mock-data.ts.
NEXT_PUBLIC_USE_LIVE_PLACES=false
